<?xml version="1.0" encoding="utf-8"?>

<layout
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <variable
            name="option"
            type="com.ranosys.theexecutive.modules.productListing.ProductListingDataClass.FilterChildOption"/>
    </data>

    <android.support.constraint.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingTop="@dimen/four_dp"
        android:paddingBottom="@dimen/four_dp">

        <TextView
            android:id="@+id/option_name_tv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{option.label}"
            android:layout_marginStart="@dimen/field_start_padding"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"/>

        <ImageView
            android:id="@+id/is_selected_iv"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:contentDescription="@string/select_option"
            android:layout_marginEnd="@dimen/thirtyTwo_dp"
            android:visibility="gone"
            app:srcCompat="@drawable/tick"
            app:layout_constraintBottom_toBottomOf="@+id/option_name_tv"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            />

    </android.support.constraint.ConstraintLayout>
</layout>