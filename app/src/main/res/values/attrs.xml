<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- ******************** Parameter Explanation ******************** -->

    <!--The maximum-->
    <!--The minimum-->
    <!--The minimum distance between two buttons-->
    <!--Cells equal to 0 for the normal mode, greater than 1 to switch to scale mode-->
    <!--the progress hint mode
    defaultMode: show hint when you move the thumb
    alwaysHide: hide progress hint all the time
    touchAlwaysShow: show progress hint all the time after touch
    alwaysShow: show progress hint all the time
    -->
    <!--The Seekbar color after dragging-->
    <!--The default Seekbar color-->
    <!--The color of the thumb when the progress is minimum or maximum, by default this property is not called-->
    <!--The color of the thumb moving, this property is not called-->
    <!--Scale text, do not set the default when hidden-->
    <!--thumb Button background resources, do not set the default when the circular button-->
    <!--Progress prompt background resources, you must use the 9 path file-->
    <!--The distance between the scale text and the progress bar-->
    <!--Scale text, and the size of the progress prompt text-->
    <!--The height of the progress prompt background, not set according to the text size adaptive-->
    <!--The with of the progress prompt background, not set according to the text size adaptive-->
    <!--The progress indicates the distance between the background and the progress bar-->
    <!--The height of the progress bar-->
    <!--The size of the button-->
    <!--Scale mode
    Number according to the scale of the actual proportion of the distribution of the location (markTextArray must be a number)
    Other bisects the current layout (markTextArray can be any character)
    -->
    <!--One-way, two-way mode single one-way mode, there is only one button range bidirectional mode, two buttons -->
    <!--the radius of progressbar-->
    <!-- ******************** Parameter Explanation ******************** -->



    <declare-styleable name="RangeSeekBar">
        <!-- the minimum of the range --><attr name="absoluteMinValue" format="integer|float"/><!-- the maximum of the range --><attr name="absoluteMaxValue" format="integer|float"/><!-- the step value --><attr name="step" format="integer|float"/><!-- show a single slider --><attr name="singleThumb" format="boolean"/><!-- show the labels on the right and left--><attr name="showLabels" format="boolean"/><!-- alwaysActive will make the buttons and bar always draw as the activeColor (the default is false) --><attr name="alwaysActive" format="boolean"/><!-- show the currently selected value above each thumb (defaults to true) --><attr name="valuesAboveThumbs" format="boolean"/><!-- padding on either side on the slider --><attr name="internalPadding" format="dimension"/><!-- the height of the bar --><attr name="barHeight" format="dimension"/><!-- the color of the bar that is not selected --><attr name="defaultColor" format="color"/><!-- the color of the bar that is selected --><attr name="activeColor" format="color"/><!-- the color of the text above thumbs --><attr name="textAboveThumbsColor" format="color"/><!-- set drawables for thumb handles --><attr name="thumbNormal" format="reference"/><attr name="thumbPressed" format="reference"/><attr name="thumbDisabled" format="reference"/><!-- thumb shadow specifications --><attr name="thumbShadow" format="boolean"/><attr name="thumbShadowColor" format="color"/><attr name="thumbShadowXOffset" format="dimension"/><attr name="thumbShadowYOffset" format="dimension"/><attr name="thumbShadowBlur" format="dimension"/><!-- to activate the thumbs and texts when the min and max values are the default ones (defaults to false) --><attr name="activateOnDefaultValues" format="boolean"/>
    </declare-styleable>
</resources>
